export const environment = {
  production: true,
  // URL de l'API backend - À MODIFIER selon votre configuration serveur
  // En production, utiliser '/api' pour le reverse proxy Nginx
  apiUrl: '/api',
  // Intervalle de rafraîchissement des actualités (en millisecondes)
  refreshInterval: 30000,
  // Nom de l'application
  appName: 'MTDPCE - Ministère de la Transition Digitale',
  // Version de l'application
  version: '1.0.0'
};
